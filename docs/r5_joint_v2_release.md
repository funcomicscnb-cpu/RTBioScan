# R5 joint-v2 deployment

The first joint-v2 deployment requires external operator drainage of all old jobs, retries, remote workers and executor queues before the new pipeline starts. Confirm that no pre-v2 worker can still mutate the shared state or publish reporting outputs. This is an operational prerequisite, not a runtime flag, configuration parameter, authority marker or computable runtime quiescence gate.

Deploy the five-file change as one coupled unit. Intermediate versions are not independently releasable. Stop and drain a dorado-only launch before starting a subsequent full-round launch. Retain displaced state and transaction evidence for diagnosis; do not delete markers, peers or individual pending obligations to force eligibility.

Completed v1 snapshots are not joint-v2 authority and restore must refuse them. A fresh authenticated full round can publish v2 only after complete scientific group validation. Genuine pristine state retains its existing behavior. There is no automatic v1 migration or fallback to older authority. A sole valid root is usable only when its peer is genuinely absent and the survivor has no transaction residue.

Dorado-only processing and presentation remain active but publish no new joint snapshot. The last authorized full-round snapshot remains the scientific restore boundary. Public figures and separately authenticated R4 reporting may be newer than that boundary.

A state-wide pending-v4 normalization witness preserves every unresolved exact round-prefix obligation, including repeated biological barcodes, replay context, stable round-JSON projections and eight absent-or-exact-byte retained-input records per round. Read-info, on-target, demult and pretax inputs must be present as safe regular files, even when empty. Recorded absence and a present-empty file are distinct. Adoption verifies all old records before writing and preserves them byte-for-byte. Legacy/v3 pending witnesses refuse. Recovery requires exact retained input bytes or recorded absence, owner-authorized whole-namespace replacement, or fresh replay; fabricating an empty file is forbidden. Restart force cannot bypass the pending, protected-state, root or R4 checks.

The candidate must pass the authorized regression, fault-injection, mutant and performance gates and receive a fresh independent exact-byte review before integration. Process-interruption safety is not a claim of power-loss durability or a cross-filesystem atomic transaction.

This continuation remains a development candidate until its completion report establishes every gate. The document does not establish release readiness on its own.
