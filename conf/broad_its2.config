/*
 * Opt-in broader ITS2 read-length preset for the default COI|ITS2 target order.
 * Use as follows:
 *   nextflow run main.nf -profile broad_its2
 */

params {
  min_read_lengths = "350|200"
  max_read_lengths = "532|800"
}
